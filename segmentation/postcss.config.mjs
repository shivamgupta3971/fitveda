/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    // No PostCSS plugins needed for this project
    // We're using inline styles instead of Tailwind
  },
};

export default config;
