/**
 * Re-export from shared module for backward compatibility.
 */
export {
  computeScore,
  buildPoseSummary,
  resetScoring,
} from "../shared/scoring";

export type {
  ScoreFrame,
  PoseSummary,
} from "../shared/scoring";
